Generate a complete correctness test for a native MUSA kernel bundle.

## Problem
{{ problem_description }}

{% if provided_test_code %}
## Reference test
```python
{{ provided_test_code }}
```
{% endif %}

Requirements:
- Return one complete fenced Python file and no prose.
- Import `torch` and `torch_musa`, then import `kernel_function` from `kernel`.
- Use device='musa' for inputs, parameters, the reference model, and outputs.
- Check availability with `torch.musa.is_available()` and synchronize with
  `torch.musa.synchronize()` when needed. Do not use torch.cuda APIs.
- Call kernel_function as a normal Python function. The native extension build
  and launch details belong to kernel.py, binding.cpp, kernel.mu, and setup.py.
- When the problem defines `get_inputs()` or `get_init_inputs()`, reproduce those
  function definitions without changing their values, shapes, dtypes, seeds, or
  argument order, and CALL them to construct the correctness workload.
- Instantiate `Model(*get_init_inputs())` (or `Model()` only when the factory is
  absent/empty), then move the returned tensor inputs to MUSA without changing
  non-tensor arguments. Pass the same logical inputs to the reference Model and
  `kernel_function`; clone tensors when independent autograd state is required.
- Do not replace the factory workload with hardcoded test tensors. Extra edge
  cases are allowed only after the shared factory workload passes.
- Reproduce the exact forward semantics in the problem. Keep reference results
  local to the test.
- Compare the native result with the PyTorch reference using rtol=1e-3 and
  atol=1e-3 for fp32, or at most rtol=1e-2 and atol=2e-2 for fp16/bf16.
- Print PASS and exit with code 0 only after numerical equivalence succeeds.
- On failure, print useful shape/dtype/error details and exit nonzero.
- Do not pass a reference model or precomputed expected output to
  kernel_function.

Generate the test now.
